#!/bin/sh
ARCH="undefined"
case $(getprop ro.product.cpu.abi) in
  arm64-v8a)
    ARCH="arm64"
  ;;
  armeabi-v7a)
    ARCH="arm"
  ;;
  armeabi)
    ARCH="arm"
  ;;
  x86)
    ARCH="x86"
  ;;
  x86_64)
    ARCH="x64"
  ;;
esac
"$0-$ARCH" --dns-servers 8.8.8.8 "$@"
